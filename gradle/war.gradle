apply plugin: "war"

bootWar {
    mainClassName = "com.especializacion.ejemploelectiva.JhipsterSampleApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
